import Vue from 'vue/dist/vue.esm';
import axios from 'axios'
import { vue_init } from '../../mixins/vue_init.js';

document.addEventListener('DOMContentLoaded', () => {
  const AccountIndexApp = new Vue({
    el: '#account-index-app',
    mixins: [vue_init],
    data: {

    },

    mounted() {

    },

    computed: {
      },

    methods: {

    }
  })
})

